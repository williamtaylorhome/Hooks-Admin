// ? Global immovable configuration items are only exported and not modified
// * Homepage address (default)
export const HOME_URL: string = "/home/index";

// * Tabs (blacklist address, route address that does not need to be added to tabs, temporarily useless)
export const TABS_BLACK_LIST: string[] = ["/403", "/404", "/500", "/layout", "/login", "/dataScreen"];

// * AutoNavi map key
export const MAP_KEY: string = "";
